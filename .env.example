# Local development (uvicorn on host → Postgres on localhost)
DATABASE_URL=postgresql://postgres:dev@localhost:5432/tasks

# Shared by docker compose (db service + api DATABASE_URL override)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dev
POSTGRES_DB=tasks
